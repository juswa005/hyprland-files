hl.config({
    input = {
        kb_layout = "us",
        follow_mouse = 1,
        sensitivity = 0,
        -- kb_options = "altwin:swap_alt_win", -- uncomment for external wireless keyboard (mac mode)

        touchpad = {
            natural_scroll = true,
            tap_to_click = true,
            disable_while_typing = true
        }
    }
})

-- hl.device({
--     name = "mouse",
--     natural_scroll = true
-- })
